# Common application properties
# https://docs.spring.io/spring-boot/docs/current/reference/html/common-application-properties.html
server:
  port: 8080
  context-path: /devtools

spring:
  devtools:
    restart:
      # 手动配置监听文件夹变化
      #additional-paths: demo-spring-boot-devtools/src/main/resources
      # 排除监听的目录
      # exclude: WEB-INF/**
  thymeleaf:
    # 页面热部署
    cache: false